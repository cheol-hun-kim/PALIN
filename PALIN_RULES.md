# 🛡️ PALIN OS 영구 절대 안전 규약 (Zero-Defect & Data-Loss Prevention Protocol)
> 본 문서는 PALIN OS 시스템의 영구 절대 원칙이며, 안티그래비티는 모든 작업 시작 전과 완료 후 반드시 본 규약을 준수하고 `qa_matrix_7gates.py`를 실행하여 검증을 증명해야 한다.

---

## 🏛️ 제1장. 인프라 & 데이터베이스 무결성 규약
1. **사일런트 폴백(Silent Fallback) 영구 금지 [절대 원칙]**
   - `DATABASE_URL`이 설정된 환경에서는 어떠한 이유로도 SQLite 로컬 임시 DB로 조용히 우회(Fallback)하는 행위를 영구 엄금한다.
   - Supabase 클라우드 DB와의 연결은 엄격 모드(Strict Mode)로 유지한다.
2. **자동 스키마 마이그레이션 (Zero-Column-Mismatch) 의무화**
   - 백엔드 모델(`models.py`)에 새 컬럼이 추가되면 서버 부팅 시 Supabase에 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`를 자동 실행하여 컬럼 누락 500 에러를 원천 차단한다.
3. **물리적 삭제 (Hard Delete) 전면 멸절**
   - 백엔드 전역에서 `DELETE` 쿼리를 금지하며, `deleted_at = datetime.now()` 논리적 삭제(Soft Delete)만 허용한다.
   - 모든 조회 API는 `deleted_at IS NULL` 필터를 강제한다.
4. **Git 저장소 DB 파일 영구 배제**
   - 깃허브 배포 시 로컬의 `*.db` 파일이 업로드되어 서버 데이터를 덮어쓰지 않도록 `.gitignore` 및 업로드 스크립트에서 영구 격리한다.
5. **전 회원(현재 180명+ 및 향후 가입할 모든 미래 회원) 데이터 전수 검토 & 라이프사이클 무결성 보장 (Universal & Future Member Lifecycle Integrity Protocol) [절대 원칙]**
   - 특정 계정(마스터 계정 포함)에 국한된 임시 예외 처리를 영구 엄금하며, 현재 데이터베이스에 존재하는 모든 회원뿐만 아니라 **앞으로 신규 가입할 모든 미래 회원에 대해서도 동일한 라이프사이클 무결성 규약(신규 가입 ➔ 첫 자습 ➔ 연속 1일, 2일... 8일, 15일, 30일 무제한 자동 승격 ➔ 최고 기록 동기화 ➔ 프로필 무결성)이 100% 프로그램적으로 강제**되어야 한다.
   - 문제 제기 시 현재 DB의 180명+ 회원뿐만 아니라, **신규 가입 파이프라인에서 생성될 미래 회원의 전체 라이프사이클(가입~학습~스트릭 누적)까지 시뮬레이션하여 결측치나 이상치가 0건임을 증명**해야 한다.

---

## 🔐 제2장. 세션 격리 & 권한 통제 규약 (Zero-Account-Leak)
1. **토큰 기반 엄격 인증 (No Lazy Auth)**
   - 브라우저의 단순 `studentId`만 보고 자동 로그인하는 로직을 금지한다. 반드시 유효한 `jwtToken`과 사용자 정보가 일치할 때만 세션을 유지하며, 불일치 시 `localStorage.clear()` 후 로그인 창을 강제 팝업한다.
2. **로그아웃 시 브라우저 완전 소멸화**
   - 로그아웃 시 `localStorage.clear()` 및 `sessionStorage.clear()`를 강제하여 타인 계정으로의 세션 오염을 0%로 차단한다.
3. **역할별 UI 권한 잠금**
   - 학생/학부모: 원장관제실 버튼 노출 완전 차단.
   - 학부모: 자녀 계획표 수정 불가 및 읽기 전용 안심 관찰 모드(`role-parent`) 강제.
   - 일반 원장: 마스터 갓모드 버튼 노출 완전 차단 (오직 총괄 마스터 `1286orbital21@gmail.com`에게만 단독 노출).

---

## 🎨 제3장. 프론트엔드 렌더링 & 이벤트 무결성 규약
1. **정적 더미 목데이터 영구 박멸 [절대 원칙]**
   - JS 배열/HTML 내에 하드코딩된 예시 랭킹(`dummyRankers`, 가짜 자습시간, 임의 학생명) 삽입을 영구 금지하며, 오직 100% 실시간 DB 집계 API 결과만 렌더링한다.
2. **모달 태그 중첩(Nesting) 및 먹통 차단 [절대 원칙]**
   - 모든 모달(`.modal`, `*-modal`)은 DOM 최상위에 독립적으로 배치되어야 하며, 닫히지 않은 태그나 `display: none` 컨테이너 내부에 중첩(Nesting)되는 행위를 원천 금지한다.
   - 모든 모달 트리거는 실제 DOM에 존재하는 ID와 1:1 매핑되어야 하며, 파서에 의해 상시 무결성이 증명되어야 한다.
3. **이벤트 중복 발화 (Double-Submit) 원천 차단**
   - HTML 인라인 `onsubmit`과 JS `addEventListener`의 중복 바인딩을 금지하고 단일 이벤트 리스너로 일원화한다.
4. **실제 가맹 기관 코드 예시 노출 영구 금지 (Zero-Real-Tenant-Leak) [절대 원칙]**
   - Placeholder 및 안내 예시 문구에 실존 기관 코드(`ILWON-2027` 등) 노출을 엄금하며, 반드시 `DAECHI-2027` 등 가상 예시만 사용한다.
5. **스트릭(연속일수) 무제한 연속성 및 실시간 동기화 보장 (Unlimited Streak Continuity)**
   - 7일 제한 없이 8일, 14일, 30일, 100일 등 모든 숫자가 정상 승격 및 표기되어야 하며, 임의 하드코딩된 fallback(예: `연속 1일`)으로 실제 DB 스트릭을 덮어쓰지 않는다.
6. **모든 인터랙티브 모달 전역 바인딩 (Zero-Undefined-Function)**
   - 모든 모달의 open/close/submit 함수는 `window.*`에 명시적으로 바인딩되어 인라인 이벤트 누락을 원천 차단한다.
7. **Day Mode(☀️ 라이트 모드) 고대비 가독성 보장 (WCAG AA 4.5:1 / Zero White-on-White)**
   - 흰색 배경 위에 흰색 글씨 출력을 영구 금지하며, 라이트 모드 시 모든 텍스트는 딥 슬레이트/고대비 컬러로 렌더링되어야 한다.

---

## ⚔️ 제4장. [7중 절대 방어선 (Absolute QA Matrix v2.5)]
- **Gate 1.1**: 하드코딩 & 목데이터(Mock Data) 원천 적발 스캐너 (더미 배열/가짜 시간 0건 강제)
- **Gate 1.2**: HTML DOM 완전 태그 밸런스(Unclosed 0건) & 모달 중첩(Nesting) 방지 검증기
- **Gate 1.3**: Anti-FOUC & MyPage 레이아웃 독립 격리 검사
- **Gate 1.4**: 실존 기관 코드 & 민감정보 Placeholder 누출 원천 차단 스캐너 (가상 예시 `DAECHI-2027` 강제)
- **Gate 2.1~2.5**: DOM 이벤트 리스너, 모달 1:1 ID 매핑, 전역 `window.*` 함수 바인딩 & 3-Persona 정보 격리 검증
- **Gate 3**: 상태 동기화 & 렌더링 지연(Stale State) 리페치 검사
- **Gate 4.1~4.3**: 동적 테마(WCAG AA 4.5:1), Zero-Flash 격리 & Day Mode 고대비(White-on-White 박멸) 검사
- **Gate 5.1~5.3**: 백엔드/프론트엔드 풀스택 컴파일 & Node.js 런타임 0-TDZ/Hoisting 검증
- **Gate 6.1~6.3**: 데이터베이스 직결, **회원정보 무유실 (Zero User Data Loss: 180명 전원 온전성)** 및 **전체 회원 전수 불꽃/프로필/성실도 일괄 정합성 전수조사 (Universal Population Audit: Zero Single-Account Bias)**
- **Gate 7.1~7.8**: 마스터 엄격 보안, Dashboard 실시간 학생 로드, Soft Delete 사이클, 신규 B2C 0분 기준, **스트릭 무제한 승격(8일, 15일, 28일)**, **실시간 Q&A 수정/삭제/환불 사이클**, **가맹 기관 연동 트랜잭션**, **원장 관제실 5종 실시간 텔레메트리 연동 (Dashboard, Notices, Attendance, Enrollment, Wifi)** E2E 풀검증 (200 OK 필수)

---

## 🚀 제5장. 실동작 E2E 시뮬레이션 및 버튼 클릭 검증 의무화 (Zero-Untested-Click Protocol) [절대 원칙]
1. **버튼/기능 완료 보고 전 '실제 인터랙션 구동' 필수 증명**
   - 새로운 버튼, 모달, 입력 폼, 슬라이드 전환 기능을 추가하거나 수정한 경우, **단순 코드 작성만으로 '완료'를 선언하는 행위를 영구 엄금**한다.
   - 반드시 다음 4단계 실동작을 프로그램적으로 직접 실행하여 성공 결과를 확인한 후에만 보고한다:
     1. **버튼 클릭 시뮬레이션**: 대상 모달의 스타일이 `display: none`에서 `display: flex/block`으로 전환되는지 확인.
     2. **슬라이드 및 폼 상호작용**: 슬라이드 다음/이전 버튼 클릭 시 액티브 인덱스가 정상 변경되고 폼 입력값이 온전히 바인딩되는지 확인.
     3. **백엔드 API 왕복 통신**: 프론트엔드가 요청하는 모든 API가 실제 백엔드에서 `200 OK` 응답과 올바른 JSON 데이터를 반환하는지 증명.
     4. **UI 실시간 반영**: API 완료 후 대상 화면(마이페이지 뱃지, 테이블 행, 랭킹 등)에 새로운 상태가 즉시 렌더링되는지 확인.
2. **미검증 기능의 사용자 배포 원천 차단**
   - `qa_matrix_7gates.py` 내의 실동작 시뮬레이션 테스트가 100% 통과하지 않은 상태에서는 어떠한 배포나 사용자 완료 보고도 허용하지 않는다.
